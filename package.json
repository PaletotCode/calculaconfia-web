{
  "name": "calculaconfia-web",
  "private": true,
  "version": "1.0.0",
  "description": "Static frontend for CalculaConfia",
  "scripts": {
    "start": "serve -s -l $PORT"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}
